moveable line endpoints
coerce pitch values into a scale
bells as circles
moveable and resizeable circles
read & write state out
different modes of ball dropping (one, three, starburst)
triggers and repeat actions and things
change the sizes of things?

bouncing off lines
close on exit
twangy lines
